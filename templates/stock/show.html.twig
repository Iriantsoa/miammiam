{% extends 'base.html.twig' %}

{% block title %}Stock{% endblock %}

{% block body %}
    <h1>Stock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock.id }}</td>
            </tr>
            <tr>
                <th>Ingredient Id</th>
                <td>{{ stock.ingredient.id }}</td>
            </tr>
            <tr>
                <th>Ingredient Name</th>
                <td>{{ stock.ingredient.nom }}</td>
            </tr>
            <tr>
                <th>Entree</th>
                <td>{{ stock.entree }}</td>
            </tr>
            <tr>
                <th>Sortie</th>
                <td>{{ stock.sortie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stock_index') }}">back to list</a>

    <a href="{{ path('app_stock_edit', {'id': stock.id}) }}">edit</a>

    {{ include('stock/_delete_form.html.twig') }}
{% endblock %}
